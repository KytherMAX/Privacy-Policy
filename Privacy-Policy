This extension processes page URLs and visible page text locally in the user's browser to detect specific keywords defined in a local rule list.

No browsing data is collected, stored, transmitted, or shared.

All processing happens locally within the user's browser.

The extension does not communicate with external servers.
